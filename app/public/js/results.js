(async()=>{let rs=[],stats={played:0,average:0,best:0};try{[rs,stats]=await Promise.all([RQ.json('/api/results'),RQ.json('/api/stats')])}catch{}const en=RQ.lang==='en';document.querySelector('#app').innerHTML=`<section class="grid stats"><div class="card stat"><strong>${stats.played}</strong><span>${en?'games':'spel'}</span></div><div class="card stat"><strong>${stats.average}%</strong><span>${en?'average':'snitt'}</span></div><div class="card stat"><strong>${stats.best}%</strong><span>${en?'best':'bäst'}</span></div></section><div class="section-title"><h2>${en?'Recent results':'Senaste resultat'}</h2></div><section class="card list">${rs.map(r=>`<div class="result-row"><div><b>${RQ.esc(r.name)}</b><br><small>${new Date(r.at).toLocaleString(en?'en-GB':'sv-SE',{dateStyle:'short',timeStyle:'short'})} · ${RQ.esc(r.mode)}</small></div><span class="score-badge">${RQ.pct(r.score,r.total)}%</span></div>`).join('')||`<div class="empty">${en?'No results yet.':'Inga resultat ännu.'}</div>`}</section>${RQ.nav('results')}`})().catch(e=>document.querySelector('#app').innerHTML=`<div class="notice error" style="margin-top:14px">${RQ.esc(e.message)}</div>${RQ.nav('results')}`);
