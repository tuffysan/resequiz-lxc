let state={qs:[],i:0,score:0,locked:false,name:RQ.name()};const app=document.querySelector('#app');
async function setup(){const meta=await RQ.json('/api/meta');app.innerHTML=`<div class="section-title"><h2>Starta soloquiz</h2></div><section class="card"><label class="field"><span>Ditt namn</span><input id="name" maxlength="30" value="${RQ.esc(state.name)}" placeholder="T.ex. Andreas"></label><label class="field"><span>Kategori</span><select id="category"><option value="">Blandat</option>${meta.categories.map(c=>`<option>${RQ.esc(c)}</option>`).join('')}</select></label><label class="field"><span>Antal frågor</span><select id="count"><option>5</option><option selected>10</option><option>15</option><option>20</option></select></label><button class="btn btn-primary btn-wide" id="start">Starta quizet →</button></section>${RQ.nav('play')}`;document.querySelector('#start').onclick=start}
async function start(){state.name=document.querySelector('#name').value.trim()||'Gäst';RQ.setName(state.name);const data=await RQ.json('/api/solo/start',{method:'POST',body:JSON.stringify({category:document.querySelector('#category').value,count:Number(document.querySelector('#count').value)})});state.qs=data.questions;state.i=0;state.score=0;show()}
function show(){const q=state.qs[state.i];state.locked=false;app.innerHTML=`<div class="section-title"><div><span class="question-kicker">Fråga ${state.i+1} av ${state.qs.length}</span></div><b>${state.score} rätt</b></div><div class="progress"><div style="width:${state.i/state.qs.length*100}%"></div></div><section class="card" style="margin-top:14px"><div class="question-kicker">${RQ.esc(q.category)} · ${RQ.esc(q.difficulty)}</div><h1 class="question-title">${RQ.esc(q.question)}</h1><div class="answers">${q.answers.map((a,i)=>`<button class="answer" data-i="${i}"><span class="letter">${'ABCD'[i]||i+1}</span><span>${RQ.esc(a)}</span></button>`).join('')}</div><div id="feedback" style="margin-top:12px"></div></section>${RQ.nav('play')}`;document.querySelectorAll('.answer').forEach(b=>b.onclick=()=>answer(Number(b.dataset.i)))}
async function answer(i){if(state.locked)return;state.locked=true;const q=state.qs[state.i],r=await RQ.json('/api/solo/check',{method:'POST',body:JSON.stringify({questionId:q.id,answerIndex:i})});if(r.correct)state.score++;document.querySelectorAll('.answer').forEach((b,x)=>{b.disabled=true;if(x===r.correctIndex)b.classList.add('correct');else if(x===i)b.classList.add('wrong')});document.querySelector('#feedback').innerHTML=`<div class="notice ${r.correct?'success':'error'}"><b>${r.correct?'Rätt!':'Inte riktigt'}</b><br>${RQ.esc(r.explanation)}</div><button class="btn btn-primary btn-wide" id="next" style="margin-top:10px">${state.i+1===state.qs.length?'Visa resultat':'Nästa fråga →'}</button>`;document.querySelector('#next').onclick=next}
async function next(){state.i++;if(state.i<state.qs.length)return show();await RQ.json('/api/results',{method:'POST',body:JSON.stringify({name:state.name,score:state.score,total:state.qs.length,mode:'solo'})});const pct=Math.round(state.score/state.qs.length*100);app.innerHTML=`<section class="hero"><h1>${pct>=80?'Snyggt jobbat!':'Quiz klart!'}</h1><p>Du fick <b>${state.score} av ${state.qs.length}</b> rätt – ${pct}%.</p><div class="actions two"><button class="btn btn-primary" id="again">Spela igen</button><a class="btn btn-secondary" href="/results.html">Se resultat</a></div></section>${RQ.nav('play')}`;document.querySelector('#again').onclick=setup}
setup().catch(e=>app.innerHTML=`<div class="notice error">${RQ.esc(e.message)}</div>${RQ.nav('play')}`);
