{
  "name": "resequiz-mobile-first",
  "version": "19.1.0",
  "private": true,
  "description": "Mobile-first travel quiz for solo and realtime multiplayer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
