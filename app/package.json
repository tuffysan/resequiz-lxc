{
  "name": "quiz",
  "version": "21.1.0",
  "private": true,
  "description": "Quiz 21.1 – Multiplayer & UX with QR lobby, display mode, offline manager and regression checks",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1",
    "qrcode": "^1.5.4"
  }
}
