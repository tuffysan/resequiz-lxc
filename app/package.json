{
  "name": "quiz",
  "version": "19.8.0",
  "private": true,
  "description": "Quiz with age-adapted children categories, realtime multiplayer, offline play and administration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
