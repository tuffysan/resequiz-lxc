{
  "name": "quiz",
  "version": "21.0.0",
  "private": true,
  "description": "Quiz 21 – Play, Progress and Quality with Daily Quiz, progression, reports and safer upgrades",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
